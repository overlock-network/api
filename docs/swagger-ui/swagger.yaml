swagger: '2.0'
info:
  title: OVERLOCK - gRPC Gateway docs
  description: A REST interface for state queries
  version: 1.0.0
paths:
  /overlock/crossplane/list_composition:
    get:
      summary: Queries a list of ListComposition items.
      operationId: Query_ListComposition
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              compositions:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                    spec:
                      type: object
                      properties:
                        composite_type_ref:
                          type: object
                          properties:
                            api_version:
                              type: string
                            kind:
                              type: string
                          description: CompositeTypeRef references a composite type.
                        mode:
                          type: string
                        resources:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                              base:
                                type: object
                                properties:
                                  api_version:
                                    type: string
                                  kind:
                                    type: string
                                description: Base defines the base resource.
                              patches:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                    from_field_path:
                                      type: string
                                    to_field_path:
                                      type: string
                                    transforms:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          map:
                                            type: string
                                          string:
                                            type: object
                                            properties:
                                              convert:
                                                type: object
                                                properties:
                                                  convert:
                                                    type: string
                                                    title: ToUpper or ToLower
                                                description: >-
                                                  StringConvert defines a string
                                                  conversion.
                                              format:
                                                type: object
                                                properties:
                                                  fmt:
                                                    type: string
                                                description: StringFormat defines a string format.
                                            description: >-
                                              StringTransform defines a string
                                              transformation.
                                          math:
                                            type: object
                                            properties:
                                              clamp_min:
                                                type: number
                                                format: double
                                              clamp_max:
                                                type: number
                                                format: double
                                              multiply:
                                                type: number
                                                format: double
                                            description: >-
                                              MathTransform defines a mathematical
                                              transformation.
                                          match:
                                            type: object
                                            properties:
                                              patterns:
                                                type: array
                                                items:
                                                  type: object
                                                  properties:
                                                    type:
                                                      type: string
                                                      title: '"literal" or "regexp"'
                                                    result:
                                                      type: string
                                                  description: >-
                                                    Pattern represents a pattern in the
                                                    match transformation.
                                              fallback_to:
                                                type: string
                                              fallback_value:
                                                type: string
                                            description: >-
                                              MatchTransform defines a match
                                              transformation.
                                          convert:
                                            type: object
                                            properties:
                                              to_type:
                                                type: string
                                                description: bool, float64, int, etc.
                                              format:
                                                type: string
                                                title: '"json" or "quantity"'
                                            description: >-
                                              ConvertTransform defines a conversion
                                              transformation.
                                        description: >-
                                          Transformer represents a transformation
                                          to be applied.
                                  description: >-
                                    Patch represents a patch to be applied to a
                                    resource.
                            description: Resource represents a resource in the composition.
                      description: Spec defines the specification of the composition.
                  description: Composition represents the composition of resources.
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListCompositionResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /overlock/crossplane/list_configuration:
    get:
      summary: Queries a list of ListConfiguration items.
      operationId: Query_ListConfiguration
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              configurations:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                    spec:
                      type: object
                      properties:
                        crossplane:
                          type: object
                          properties:
                            version:
                              type: string
                          description: Crossplane specs.
                        compositions:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: compositions
                        xrds:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: xrds
                        functions:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: functions
                        providers:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: providers
                        configurations:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: configurations
                      description: Spec defines the specification of the composition.
                    creator:
                      type: string
                  description: Configuration holds the configuration details.
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListConfigurationResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
        - name: creator
          in: query
          required: false
          type: string
      tags:
        - Query
  /overlock/crossplane/list_environment:
    get:
      summary: Queries a list of ListEnvironment items.
      operationId: Query_ListEnvironment
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              environments:
                type: array
                items:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                      title: metadata
                    spec:
                      title: spec
                      type: object
                      properties:
                        providers:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: providers
                        configurations:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: configurations
                        functions:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: functions
                        registries:
                          type: array
                          items:
                            type: string
                            format: uint64
                      description: Spec defines the specification of the environment.
                    id:
                      type: string
                      format: uint64
                      title: id
                    creator:
                      type: string
                      title: creator
                  title: Environment
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListEnvironmentResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
        - name: creator
          in: query
          required: false
          type: string
      tags:
        - Query
  /overlock/crossplane/list_function:
    get:
      summary: Queries a list of ListFunction items.
      operationId: Query_ListFunction
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              functions:
                type: array
                items:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                      title: name
                    spec:
                      title: spec
                      type: object
                      properties:
                        package:
                          type: string
                          title: package
                      description: Spec defines the specification of the function.
                    id:
                      type: string
                      format: uint64
                      title: id
                    creator:
                      type: string
                      title: creator
                  title: Function
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListFunctionResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
        - name: creator
          in: query
          required: false
          type: string
      tags:
        - Query
  /overlock/crossplane/list_provider:
    get:
      summary: Queries a list of ListProvider items.
      operationId: Query_ListProvider
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              providers:
                type: array
                items:
                  type: object
                  properties:
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                      title: name
                    spec:
                      title: spec
                      type: object
                      properties:
                        package:
                          type: string
                          title: package
                      description: Spec defines the specification of the provider.
                    id:
                      type: string
                      format: uint64
                      title: id
                    creator:
                      type: string
                      title: creator
                  title: Provider
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListProviderResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
        - name: creator
          in: query
          required: false
          type: string
      tags:
        - Query
  /overlock/crossplane/list_xrd:
    get:
      summary: Queries a list of ListXrd items.
      operationId: Query_ListXrd
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              xrd:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    metadata:
                      type: object
                      properties:
                        name:
                          type: string
                        annotations:
                          type: string
                      description: Metadata contains metadata information.
                    spec:
                      type: object
                      properties:
                        group:
                          type: string
                        names:
                          type: object
                          properties:
                            kind:
                              type: string
                            plural:
                              type: string
                          description: >-
                            Names of the resource in singular, plural, and kind
                            forms.
                        versions:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                              schema:
                                type: string
                            description: Defines each version of the CRD.
                      description: Main CompositeResourceDefinition spec structure.
                  description: Complete CompositeResourceDefinition object.
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: QueryListXrdResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /overlock/crossplane/show_composition/{id}:
    get:
      summary: Queries a list of ShowComposition items.
      operationId: Query_ShowComposition
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              composition:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                  spec:
                    type: object
                    properties:
                      composite_type_ref:
                        type: object
                        properties:
                          api_version:
                            type: string
                          kind:
                            type: string
                        description: CompositeTypeRef references a composite type.
                      mode:
                        type: string
                      resources:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            base:
                              type: object
                              properties:
                                api_version:
                                  type: string
                                kind:
                                  type: string
                              description: Base defines the base resource.
                            patches:
                              type: array
                              items:
                                type: object
                                properties:
                                  type:
                                    type: string
                                  from_field_path:
                                    type: string
                                  to_field_path:
                                    type: string
                                  transforms:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        map:
                                          type: string
                                        string:
                                          type: object
                                          properties:
                                            convert:
                                              type: object
                                              properties:
                                                convert:
                                                  type: string
                                                  title: ToUpper or ToLower
                                              description: >-
                                                StringConvert defines a string
                                                conversion.
                                            format:
                                              type: object
                                              properties:
                                                fmt:
                                                  type: string
                                              description: StringFormat defines a string format.
                                          description: >-
                                            StringTransform defines a string
                                            transformation.
                                        math:
                                          type: object
                                          properties:
                                            clamp_min:
                                              type: number
                                              format: double
                                            clamp_max:
                                              type: number
                                              format: double
                                            multiply:
                                              type: number
                                              format: double
                                          description: >-
                                            MathTransform defines a mathematical
                                            transformation.
                                        match:
                                          type: object
                                          properties:
                                            patterns:
                                              type: array
                                              items:
                                                type: object
                                                properties:
                                                  type:
                                                    type: string
                                                    title: '"literal" or "regexp"'
                                                  result:
                                                    type: string
                                                description: >-
                                                  Pattern represents a pattern in the
                                                  match transformation.
                                            fallback_to:
                                              type: string
                                            fallback_value:
                                              type: string
                                          description: >-
                                            MatchTransform defines a match
                                            transformation.
                                        convert:
                                          type: object
                                          properties:
                                            to_type:
                                              type: string
                                              description: bool, float64, int, etc.
                                            format:
                                              type: string
                                              title: '"json" or "quantity"'
                                          description: >-
                                            ConvertTransform defines a conversion
                                            transformation.
                                      description: >-
                                        Transformer represents a transformation
                                        to be applied.
                                description: >-
                                  Patch represents a patch to be applied to a
                                  resource.
                          description: Resource represents a resource in the composition.
                    description: Spec defines the specification of the composition.
                description: Composition represents the composition of resources.
            title: QueryShowCompositionResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/crossplane/show_configuration/{id}:
    get:
      summary: Queries a list of ShowConfiguration items.
      operationId: Query_ShowConfiguration
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              configuration:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                  spec:
                    type: object
                    properties:
                      crossplane:
                        type: object
                        properties:
                          version:
                            type: string
                        description: Crossplane specs.
                      compositions:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: compositions
                      xrds:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: xrds
                      functions:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: functions
                      providers:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: providers
                      configurations:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: configurations
                    description: Spec defines the specification of the composition.
                  creator:
                    type: string
                description: Configuration holds the configuration details.
            title: QueryShowConfigurationResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/crossplane/show_environment/{id}:
    get:
      summary: Queries a list of ShowEnvironment items.
      operationId: Query_ShowEnvironment
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              environment:
                type: object
                properties:
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                    title: metadata
                  spec:
                    title: spec
                    type: object
                    properties:
                      providers:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: providers
                      configurations:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: configurations
                      functions:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: functions
                      registries:
                        type: array
                        items:
                          type: string
                          format: uint64
                    description: Spec defines the specification of the environment.
                  id:
                    type: string
                    format: uint64
                    title: id
                  creator:
                    type: string
                    title: creator
                title: Environment
            title: QueryShowEnvironmentResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/crossplane/show_function/{id}:
    get:
      summary: Queries a list of ShowFunction items.
      operationId: Query_ShowFunction
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              function:
                type: object
                properties:
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                    title: name
                  spec:
                    title: spec
                    type: object
                    properties:
                      package:
                        type: string
                        title: package
                    description: Spec defines the specification of the function.
                  id:
                    type: string
                    format: uint64
                    title: id
                  creator:
                    type: string
                    title: creator
                title: Function
            title: QueryShowFunctionResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/crossplane/show_provider/{id}:
    get:
      summary: Queries a list of ShowProvider items.
      operationId: Query_ShowProvider
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              provider:
                type: object
                properties:
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                    title: name
                  spec:
                    title: spec
                    type: object
                    properties:
                      package:
                        type: string
                        title: package
                    description: Spec defines the specification of the provider.
                  id:
                    type: string
                    format: uint64
                    title: id
                  creator:
                    type: string
                    title: creator
                title: Provider
            title: QueryShowProviderResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/crossplane/show_xrd/{id}:
    get:
      summary: Queries a list of ShowXrd items.
      operationId: Query_ShowXrd
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              xrd:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  metadata:
                    type: object
                    properties:
                      name:
                        type: string
                      annotations:
                        type: string
                    description: Metadata contains metadata information.
                  spec:
                    type: object
                    properties:
                      group:
                        type: string
                      names:
                        type: object
                        properties:
                          kind:
                            type: string
                          plural:
                            type: string
                        description: >-
                          Names of the resource in singular, plural, and kind
                          forms.
                      versions:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            schema:
                              type: string
                          description: Defines each version of the CRD.
                    description: Main CompositeResourceDefinition spec structure.
                description: Complete CompositeResourceDefinition object.
            title: QueryShowXrdResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /overlock/storage/list_registry:
    get:
      summary: Queries a list of ListRegistry items.
      operationId: Query_ListRegistry
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              registry:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    provider:
                      type: string
                    id:
                      type: string
                      format: uint64
                    environment_id:
                      type: string
                      format: uint64
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /overlock/storage/show_registry/{id}:
    get:
      summary: Queries a list of ShowRegistry items.
      operationId: Query_ShowRegistry
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              registry:
                type: object
                properties:
                  name:
                    type: string
                  provider:
                    type: string
                  id:
                    type: string
                    format: uint64
                  environment_id:
                    type: string
                    format: uint64
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
definitions:
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
    additionalProperties: {}
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
          additionalProperties: {}
  v1beta1Base:
    type: object
    properties:
      api_version:
        type: string
      kind:
        type: string
    description: Base defines the base resource.
  v1beta1CompositeResourceDefinition:
    type: object
    properties:
      id:
        type: string
        format: uint64
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
      spec:
        type: object
        properties:
          group:
            type: string
          names:
            type: object
            properties:
              kind:
                type: string
              plural:
                type: string
            description: Names of the resource in singular, plural, and kind forms.
          versions:
            type: array
            items:
              type: object
              properties:
                name:
                  type: string
                schema:
                  type: string
              description: Defines each version of the CRD.
        description: Main CompositeResourceDefinition spec structure.
    description: Complete CompositeResourceDefinition object.
  v1beta1CompositeTypeRef:
    type: object
    properties:
      api_version:
        type: string
      kind:
        type: string
    description: CompositeTypeRef references a composite type.
  v1beta1Composition:
    type: object
    properties:
      id:
        type: string
        format: uint64
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
      spec:
        type: object
        properties:
          composite_type_ref:
            type: object
            properties:
              api_version:
                type: string
              kind:
                type: string
            description: CompositeTypeRef references a composite type.
          mode:
            type: string
          resources:
            type: array
            items:
              type: object
              properties:
                name:
                  type: string
                base:
                  type: object
                  properties:
                    api_version:
                      type: string
                    kind:
                      type: string
                  description: Base defines the base resource.
                patches:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      from_field_path:
                        type: string
                      to_field_path:
                        type: string
                      transforms:
                        type: array
                        items:
                          type: object
                          properties:
                            map:
                              type: string
                            string:
                              type: object
                              properties:
                                convert:
                                  type: object
                                  properties:
                                    convert:
                                      type: string
                                      title: ToUpper or ToLower
                                  description: StringConvert defines a string conversion.
                                format:
                                  type: object
                                  properties:
                                    fmt:
                                      type: string
                                  description: StringFormat defines a string format.
                              description: StringTransform defines a string transformation.
                            math:
                              type: object
                              properties:
                                clamp_min:
                                  type: number
                                  format: double
                                clamp_max:
                                  type: number
                                  format: double
                                multiply:
                                  type: number
                                  format: double
                              description: >-
                                MathTransform defines a mathematical
                                transformation.
                            match:
                              type: object
                              properties:
                                patterns:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      type:
                                        type: string
                                        title: '"literal" or "regexp"'
                                      result:
                                        type: string
                                    description: >-
                                      Pattern represents a pattern in the match
                                      transformation.
                                fallback_to:
                                  type: string
                                fallback_value:
                                  type: string
                              description: MatchTransform defines a match transformation.
                            convert:
                              type: object
                              properties:
                                to_type:
                                  type: string
                                  description: bool, float64, int, etc.
                                format:
                                  type: string
                                  title: '"json" or "quantity"'
                              description: >-
                                ConvertTransform defines a conversion
                                transformation.
                          description: >-
                            Transformer represents a transformation to be
                            applied.
                    description: Patch represents a patch to be applied to a resource.
              description: Resource represents a resource in the composition.
        description: Spec defines the specification of the composition.
    description: Composition represents the composition of resources.
  v1beta1CompositionSpec:
    type: object
    properties:
      composite_type_ref:
        type: object
        properties:
          api_version:
            type: string
          kind:
            type: string
        description: CompositeTypeRef references a composite type.
      mode:
        type: string
      resources:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
            base:
              type: object
              properties:
                api_version:
                  type: string
                kind:
                  type: string
              description: Base defines the base resource.
            patches:
              type: array
              items:
                type: object
                properties:
                  type:
                    type: string
                  from_field_path:
                    type: string
                  to_field_path:
                    type: string
                  transforms:
                    type: array
                    items:
                      type: object
                      properties:
                        map:
                          type: string
                        string:
                          type: object
                          properties:
                            convert:
                              type: object
                              properties:
                                convert:
                                  type: string
                                  title: ToUpper or ToLower
                              description: StringConvert defines a string conversion.
                            format:
                              type: object
                              properties:
                                fmt:
                                  type: string
                              description: StringFormat defines a string format.
                          description: StringTransform defines a string transformation.
                        math:
                          type: object
                          properties:
                            clamp_min:
                              type: number
                              format: double
                            clamp_max:
                              type: number
                              format: double
                            multiply:
                              type: number
                              format: double
                          description: MathTransform defines a mathematical transformation.
                        match:
                          type: object
                          properties:
                            patterns:
                              type: array
                              items:
                                type: object
                                properties:
                                  type:
                                    type: string
                                    title: '"literal" or "regexp"'
                                  result:
                                    type: string
                                description: >-
                                  Pattern represents a pattern in the match
                                  transformation.
                            fallback_to:
                              type: string
                            fallback_value:
                              type: string
                          description: MatchTransform defines a match transformation.
                        convert:
                          type: object
                          properties:
                            to_type:
                              type: string
                              description: bool, float64, int, etc.
                            format:
                              type: string
                              title: '"json" or "quantity"'
                          description: >-
                            ConvertTransform defines a conversion
                            transformation.
                      description: Transformer represents a transformation to be applied.
                description: Patch represents a patch to be applied to a resource.
          description: Resource represents a resource in the composition.
    description: Spec defines the specification of the composition.
  v1beta1Configuration:
    type: object
    properties:
      id:
        type: string
        format: uint64
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
      spec:
        type: object
        properties:
          crossplane:
            type: object
            properties:
              version:
                type: string
            description: Crossplane specs.
          compositions:
            type: array
            items:
              type: string
              format: uint64
            title: compositions
          xrds:
            type: array
            items:
              type: string
              format: uint64
            title: xrds
          functions:
            type: array
            items:
              type: string
              format: uint64
            title: functions
          providers:
            type: array
            items:
              type: string
              format: uint64
            title: providers
          configurations:
            type: array
            items:
              type: string
              format: uint64
            title: configurations
        description: Spec defines the specification of the composition.
      creator:
        type: string
    description: Configuration holds the configuration details.
  v1beta1ConfigurationSpec:
    type: object
    properties:
      crossplane:
        type: object
        properties:
          version:
            type: string
        description: Crossplane specs.
      compositions:
        type: array
        items:
          type: string
          format: uint64
        title: compositions
      xrds:
        type: array
        items:
          type: string
          format: uint64
        title: xrds
      functions:
        type: array
        items:
          type: string
          format: uint64
        title: functions
      providers:
        type: array
        items:
          type: string
          format: uint64
        title: providers
      configurations:
        type: array
        items:
          type: string
          format: uint64
        title: configurations
    description: Spec defines the specification of the composition.
  v1beta1ConvertTransform:
    type: object
    properties:
      to_type:
        type: string
        description: bool, float64, int, etc.
      format:
        type: string
        title: '"json" or "quantity"'
    description: ConvertTransform defines a conversion transformation.
  v1beta1CrossplaneSpec:
    type: object
    properties:
      version:
        type: string
    description: Crossplane specs.
  v1beta1Environment:
    type: object
    properties:
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
        title: metadata
      spec:
        title: spec
        type: object
        properties:
          providers:
            type: array
            items:
              type: string
              format: uint64
            title: providers
          configurations:
            type: array
            items:
              type: string
              format: uint64
            title: configurations
          functions:
            type: array
            items:
              type: string
              format: uint64
            title: functions
          registries:
            type: array
            items:
              type: string
              format: uint64
        description: Spec defines the specification of the environment.
      id:
        type: string
        format: uint64
        title: id
      creator:
        type: string
        title: creator
    title: Environment
  v1beta1EnvironmentSpec:
    type: object
    properties:
      providers:
        type: array
        items:
          type: string
          format: uint64
        title: providers
      configurations:
        type: array
        items:
          type: string
          format: uint64
        title: configurations
      functions:
        type: array
        items:
          type: string
          format: uint64
        title: functions
      registries:
        type: array
        items:
          type: string
          format: uint64
    description: Spec defines the specification of the environment.
  v1beta1Function:
    type: object
    properties:
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
        title: name
      spec:
        title: spec
        type: object
        properties:
          package:
            type: string
            title: package
        description: Spec defines the specification of the function.
      id:
        type: string
        format: uint64
        title: id
      creator:
        type: string
        title: creator
    title: Function
  v1beta1FunctionSpec:
    type: object
    properties:
      package:
        type: string
        title: package
    description: Spec defines the specification of the function.
  v1beta1MatchTransform:
    type: object
    properties:
      patterns:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
              title: '"literal" or "regexp"'
            result:
              type: string
          description: Pattern represents a pattern in the match transformation.
      fallback_to:
        type: string
      fallback_value:
        type: string
    description: MatchTransform defines a match transformation.
  v1beta1MathTransform:
    type: object
    properties:
      clamp_min:
        type: number
        format: double
      clamp_max:
        type: number
        format: double
      multiply:
        type: number
        format: double
    description: MathTransform defines a mathematical transformation.
  v1beta1Metadata:
    type: object
    properties:
      name:
        type: string
      annotations:
        type: string
    description: Metadata contains metadata information.
  v1beta1PageRequest:
    type: object
    properties:
      key:
        type: string
        format: byte
        description: |-
          key is a value returned in PageResponse.next_key to begin
          querying the next page most efficiently. Only one of offset or key
          should be set.
      offset:
        type: string
        format: uint64
        description: |-
          offset is a numeric offset that can be used when key is unavailable.
          It is less efficient than using key. Only one of offset or key should
          be set.
      limit:
        type: string
        format: uint64
        description: >-
          limit is the total number of results to be returned in the result
          page.

          If left empty it will default to a value to be set by each app.
      count_total:
        type: boolean
        description: >-
          count_total is set to true  to indicate that the result set should
          include

          a count of the total number of items available for pagination in UIs.

          count_total is only respected when offset is used. It is ignored when
          key

          is set.
      reverse:
        type: boolean
        description: >-
          reverse is set to true if results are to be returned in the descending
          order.


          Since: cosmos-sdk 0.43
    description: |-
      message SomeRequest {
               Foo some_parameter = 1;
               PageRequest pagination = 2;
       }
    title: |-
      PageRequest is to be embedded in gRPC request messages for efficient
      pagination. Ex:
  v1beta1PageResponse:
    type: object
    properties:
      next_key:
        type: string
        format: byte
        description: |-
          next_key is the key to be passed to PageRequest.key to
          query the next page most efficiently. It will be empty if
          there are no more results.
      total:
        type: string
        format: uint64
        title: |-
          total is total number of results available if PageRequest.count_total
          was set, its value is undefined otherwise
    description: |-
      PageResponse is to be embedded in gRPC response messages where the
      corresponding request message has used PageRequest.

       message SomeResponse {
               repeated Bar results = 1;
               PageResponse page = 2;
       }
  v1beta1Patch:
    type: object
    properties:
      type:
        type: string
      from_field_path:
        type: string
      to_field_path:
        type: string
      transforms:
        type: array
        items:
          type: object
          properties:
            map:
              type: string
            string:
              type: object
              properties:
                convert:
                  type: object
                  properties:
                    convert:
                      type: string
                      title: ToUpper or ToLower
                  description: StringConvert defines a string conversion.
                format:
                  type: object
                  properties:
                    fmt:
                      type: string
                  description: StringFormat defines a string format.
              description: StringTransform defines a string transformation.
            math:
              type: object
              properties:
                clamp_min:
                  type: number
                  format: double
                clamp_max:
                  type: number
                  format: double
                multiply:
                  type: number
                  format: double
              description: MathTransform defines a mathematical transformation.
            match:
              type: object
              properties:
                patterns:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                        title: '"literal" or "regexp"'
                      result:
                        type: string
                    description: Pattern represents a pattern in the match transformation.
                fallback_to:
                  type: string
                fallback_value:
                  type: string
              description: MatchTransform defines a match transformation.
            convert:
              type: object
              properties:
                to_type:
                  type: string
                  description: bool, float64, int, etc.
                format:
                  type: string
                  title: '"json" or "quantity"'
              description: ConvertTransform defines a conversion transformation.
          description: Transformer represents a transformation to be applied.
    description: Patch represents a patch to be applied to a resource.
  v1beta1Pattern:
    type: object
    properties:
      type:
        type: string
        title: '"literal" or "regexp"'
      result:
        type: string
    description: Pattern represents a pattern in the match transformation.
  v1beta1Provider:
    type: object
    properties:
      metadata:
        type: object
        properties:
          name:
            type: string
          annotations:
            type: string
        description: Metadata contains metadata information.
        title: name
      spec:
        title: spec
        type: object
        properties:
          package:
            type: string
            title: package
        description: Spec defines the specification of the provider.
      id:
        type: string
        format: uint64
        title: id
      creator:
        type: string
        title: creator
    title: Provider
  v1beta1ProviderSpec:
    type: object
    properties:
      package:
        type: string
        title: package
    description: Spec defines the specification of the provider.
  v1beta1QueryListCompositionResponse:
    type: object
    properties:
      compositions:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
            spec:
              type: object
              properties:
                composite_type_ref:
                  type: object
                  properties:
                    api_version:
                      type: string
                    kind:
                      type: string
                  description: CompositeTypeRef references a composite type.
                mode:
                  type: string
                resources:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      base:
                        type: object
                        properties:
                          api_version:
                            type: string
                          kind:
                            type: string
                        description: Base defines the base resource.
                      patches:
                        type: array
                        items:
                          type: object
                          properties:
                            type:
                              type: string
                            from_field_path:
                              type: string
                            to_field_path:
                              type: string
                            transforms:
                              type: array
                              items:
                                type: object
                                properties:
                                  map:
                                    type: string
                                  string:
                                    type: object
                                    properties:
                                      convert:
                                        type: object
                                        properties:
                                          convert:
                                            type: string
                                            title: ToUpper or ToLower
                                        description: >-
                                          StringConvert defines a string
                                          conversion.
                                      format:
                                        type: object
                                        properties:
                                          fmt:
                                            type: string
                                        description: StringFormat defines a string format.
                                    description: >-
                                      StringTransform defines a string
                                      transformation.
                                  math:
                                    type: object
                                    properties:
                                      clamp_min:
                                        type: number
                                        format: double
                                      clamp_max:
                                        type: number
                                        format: double
                                      multiply:
                                        type: number
                                        format: double
                                    description: >-
                                      MathTransform defines a mathematical
                                      transformation.
                                  match:
                                    type: object
                                    properties:
                                      patterns:
                                        type: array
                                        items:
                                          type: object
                                          properties:
                                            type:
                                              type: string
                                              title: '"literal" or "regexp"'
                                            result:
                                              type: string
                                          description: >-
                                            Pattern represents a pattern in the
                                            match transformation.
                                      fallback_to:
                                        type: string
                                      fallback_value:
                                        type: string
                                    description: >-
                                      MatchTransform defines a match
                                      transformation.
                                  convert:
                                    type: object
                                    properties:
                                      to_type:
                                        type: string
                                        description: bool, float64, int, etc.
                                      format:
                                        type: string
                                        title: '"json" or "quantity"'
                                    description: >-
                                      ConvertTransform defines a conversion
                                      transformation.
                                description: >-
                                  Transformer represents a transformation to be
                                  applied.
                          description: >-
                            Patch represents a patch to be applied to a
                            resource.
                    description: Resource represents a resource in the composition.
              description: Spec defines the specification of the composition.
          description: Composition represents the composition of resources.
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListCompositionResponse
  v1beta1QueryListConfigurationResponse:
    type: object
    properties:
      configurations:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
            spec:
              type: object
              properties:
                crossplane:
                  type: object
                  properties:
                    version:
                      type: string
                  description: Crossplane specs.
                compositions:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: compositions
                xrds:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: xrds
                functions:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: functions
                providers:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: providers
                configurations:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: configurations
              description: Spec defines the specification of the composition.
            creator:
              type: string
          description: Configuration holds the configuration details.
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListConfigurationResponse
  v1beta1QueryListEnvironmentResponse:
    type: object
    properties:
      environments:
        type: array
        items:
          type: object
          properties:
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
              title: metadata
            spec:
              title: spec
              type: object
              properties:
                providers:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: providers
                configurations:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: configurations
                functions:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: functions
                registries:
                  type: array
                  items:
                    type: string
                    format: uint64
              description: Spec defines the specification of the environment.
            id:
              type: string
              format: uint64
              title: id
            creator:
              type: string
              title: creator
          title: Environment
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListEnvironmentResponse
  v1beta1QueryListFunctionResponse:
    type: object
    properties:
      functions:
        type: array
        items:
          type: object
          properties:
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
              title: name
            spec:
              title: spec
              type: object
              properties:
                package:
                  type: string
                  title: package
              description: Spec defines the specification of the function.
            id:
              type: string
              format: uint64
              title: id
            creator:
              type: string
              title: creator
          title: Function
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListFunctionResponse
  v1beta1QueryListProviderResponse:
    type: object
    properties:
      providers:
        type: array
        items:
          type: object
          properties:
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
              title: name
            spec:
              title: spec
              type: object
              properties:
                package:
                  type: string
                  title: package
              description: Spec defines the specification of the provider.
            id:
              type: string
              format: uint64
              title: id
            creator:
              type: string
              title: creator
          title: Provider
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListProviderResponse
  v1beta1QueryListXrdResponse:
    type: object
    properties:
      xrd:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            metadata:
              type: object
              properties:
                name:
                  type: string
                annotations:
                  type: string
              description: Metadata contains metadata information.
            spec:
              type: object
              properties:
                group:
                  type: string
                names:
                  type: object
                  properties:
                    kind:
                      type: string
                    plural:
                      type: string
                  description: Names of the resource in singular, plural, and kind forms.
                versions:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      schema:
                        type: string
                    description: Defines each version of the CRD.
              description: Main CompositeResourceDefinition spec structure.
          description: Complete CompositeResourceDefinition object.
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: QueryListXrdResponse
  v1beta1QueryShowCompositionResponse:
    type: object
    properties:
      composition:
        type: object
        properties:
          id:
            type: string
            format: uint64
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
          spec:
            type: object
            properties:
              composite_type_ref:
                type: object
                properties:
                  api_version:
                    type: string
                  kind:
                    type: string
                description: CompositeTypeRef references a composite type.
              mode:
                type: string
              resources:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    base:
                      type: object
                      properties:
                        api_version:
                          type: string
                        kind:
                          type: string
                      description: Base defines the base resource.
                    patches:
                      type: array
                      items:
                        type: object
                        properties:
                          type:
                            type: string
                          from_field_path:
                            type: string
                          to_field_path:
                            type: string
                          transforms:
                            type: array
                            items:
                              type: object
                              properties:
                                map:
                                  type: string
                                string:
                                  type: object
                                  properties:
                                    convert:
                                      type: object
                                      properties:
                                        convert:
                                          type: string
                                          title: ToUpper or ToLower
                                      description: >-
                                        StringConvert defines a string
                                        conversion.
                                    format:
                                      type: object
                                      properties:
                                        fmt:
                                          type: string
                                      description: StringFormat defines a string format.
                                  description: >-
                                    StringTransform defines a string
                                    transformation.
                                math:
                                  type: object
                                  properties:
                                    clamp_min:
                                      type: number
                                      format: double
                                    clamp_max:
                                      type: number
                                      format: double
                                    multiply:
                                      type: number
                                      format: double
                                  description: >-
                                    MathTransform defines a mathematical
                                    transformation.
                                match:
                                  type: object
                                  properties:
                                    patterns:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          type:
                                            type: string
                                            title: '"literal" or "regexp"'
                                          result:
                                            type: string
                                        description: >-
                                          Pattern represents a pattern in the
                                          match transformation.
                                    fallback_to:
                                      type: string
                                    fallback_value:
                                      type: string
                                  description: >-
                                    MatchTransform defines a match
                                    transformation.
                                convert:
                                  type: object
                                  properties:
                                    to_type:
                                      type: string
                                      description: bool, float64, int, etc.
                                    format:
                                      type: string
                                      title: '"json" or "quantity"'
                                  description: >-
                                    ConvertTransform defines a conversion
                                    transformation.
                              description: >-
                                Transformer represents a transformation to be
                                applied.
                        description: Patch represents a patch to be applied to a resource.
                  description: Resource represents a resource in the composition.
            description: Spec defines the specification of the composition.
        description: Composition represents the composition of resources.
    title: QueryShowCompositionResponse
  v1beta1QueryShowConfigurationResponse:
    type: object
    properties:
      configuration:
        type: object
        properties:
          id:
            type: string
            format: uint64
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
          spec:
            type: object
            properties:
              crossplane:
                type: object
                properties:
                  version:
                    type: string
                description: Crossplane specs.
              compositions:
                type: array
                items:
                  type: string
                  format: uint64
                title: compositions
              xrds:
                type: array
                items:
                  type: string
                  format: uint64
                title: xrds
              functions:
                type: array
                items:
                  type: string
                  format: uint64
                title: functions
              providers:
                type: array
                items:
                  type: string
                  format: uint64
                title: providers
              configurations:
                type: array
                items:
                  type: string
                  format: uint64
                title: configurations
            description: Spec defines the specification of the composition.
          creator:
            type: string
        description: Configuration holds the configuration details.
    title: QueryShowConfigurationResponse
  v1beta1QueryShowEnvironmentResponse:
    type: object
    properties:
      environment:
        type: object
        properties:
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
            title: metadata
          spec:
            title: spec
            type: object
            properties:
              providers:
                type: array
                items:
                  type: string
                  format: uint64
                title: providers
              configurations:
                type: array
                items:
                  type: string
                  format: uint64
                title: configurations
              functions:
                type: array
                items:
                  type: string
                  format: uint64
                title: functions
              registries:
                type: array
                items:
                  type: string
                  format: uint64
            description: Spec defines the specification of the environment.
          id:
            type: string
            format: uint64
            title: id
          creator:
            type: string
            title: creator
        title: Environment
    title: QueryShowEnvironmentResponse
  v1beta1QueryShowFunctionResponse:
    type: object
    properties:
      function:
        type: object
        properties:
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
            title: name
          spec:
            title: spec
            type: object
            properties:
              package:
                type: string
                title: package
            description: Spec defines the specification of the function.
          id:
            type: string
            format: uint64
            title: id
          creator:
            type: string
            title: creator
        title: Function
    title: QueryShowFunctionResponse
  v1beta1QueryShowProviderResponse:
    type: object
    properties:
      provider:
        type: object
        properties:
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
            title: name
          spec:
            title: spec
            type: object
            properties:
              package:
                type: string
                title: package
            description: Spec defines the specification of the provider.
          id:
            type: string
            format: uint64
            title: id
          creator:
            type: string
            title: creator
        title: Provider
    title: QueryShowProviderResponse
  v1beta1QueryShowXrdResponse:
    type: object
    properties:
      xrd:
        type: object
        properties:
          id:
            type: string
            format: uint64
          metadata:
            type: object
            properties:
              name:
                type: string
              annotations:
                type: string
            description: Metadata contains metadata information.
          spec:
            type: object
            properties:
              group:
                type: string
              names:
                type: object
                properties:
                  kind:
                    type: string
                  plural:
                    type: string
                description: Names of the resource in singular, plural, and kind forms.
              versions:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    schema:
                      type: string
                  description: Defines each version of the CRD.
            description: Main CompositeResourceDefinition spec structure.
        description: Complete CompositeResourceDefinition object.
    title: QueryShowXrdResponse
  v1beta1Resource:
    type: object
    properties:
      name:
        type: string
      base:
        type: object
        properties:
          api_version:
            type: string
          kind:
            type: string
        description: Base defines the base resource.
      patches:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
            from_field_path:
              type: string
            to_field_path:
              type: string
            transforms:
              type: array
              items:
                type: object
                properties:
                  map:
                    type: string
                  string:
                    type: object
                    properties:
                      convert:
                        type: object
                        properties:
                          convert:
                            type: string
                            title: ToUpper or ToLower
                        description: StringConvert defines a string conversion.
                      format:
                        type: object
                        properties:
                          fmt:
                            type: string
                        description: StringFormat defines a string format.
                    description: StringTransform defines a string transformation.
                  math:
                    type: object
                    properties:
                      clamp_min:
                        type: number
                        format: double
                      clamp_max:
                        type: number
                        format: double
                      multiply:
                        type: number
                        format: double
                    description: MathTransform defines a mathematical transformation.
                  match:
                    type: object
                    properties:
                      patterns:
                        type: array
                        items:
                          type: object
                          properties:
                            type:
                              type: string
                              title: '"literal" or "regexp"'
                            result:
                              type: string
                          description: >-
                            Pattern represents a pattern in the match
                            transformation.
                      fallback_to:
                        type: string
                      fallback_value:
                        type: string
                    description: MatchTransform defines a match transformation.
                  convert:
                    type: object
                    properties:
                      to_type:
                        type: string
                        description: bool, float64, int, etc.
                      format:
                        type: string
                        title: '"json" or "quantity"'
                    description: ConvertTransform defines a conversion transformation.
                description: Transformer represents a transformation to be applied.
          description: Patch represents a patch to be applied to a resource.
    description: Resource represents a resource in the composition.
  v1beta1ResourceNames:
    type: object
    properties:
      kind:
        type: string
      plural:
        type: string
    description: Names of the resource in singular, plural, and kind forms.
  v1beta1StringConvert:
    type: object
    properties:
      convert:
        type: string
        title: ToUpper or ToLower
    description: StringConvert defines a string conversion.
  v1beta1StringFormat:
    type: object
    properties:
      fmt:
        type: string
    description: StringFormat defines a string format.
  v1beta1StringTransform:
    type: object
    properties:
      convert:
        type: object
        properties:
          convert:
            type: string
            title: ToUpper or ToLower
        description: StringConvert defines a string conversion.
      format:
        type: object
        properties:
          fmt:
            type: string
        description: StringFormat defines a string format.
    description: StringTransform defines a string transformation.
  v1beta1Transformer:
    type: object
    properties:
      map:
        type: string
      string:
        type: object
        properties:
          convert:
            type: object
            properties:
              convert:
                type: string
                title: ToUpper or ToLower
            description: StringConvert defines a string conversion.
          format:
            type: object
            properties:
              fmt:
                type: string
            description: StringFormat defines a string format.
        description: StringTransform defines a string transformation.
      math:
        type: object
        properties:
          clamp_min:
            type: number
            format: double
          clamp_max:
            type: number
            format: double
          multiply:
            type: number
            format: double
        description: MathTransform defines a mathematical transformation.
      match:
        type: object
        properties:
          patterns:
            type: array
            items:
              type: object
              properties:
                type:
                  type: string
                  title: '"literal" or "regexp"'
                result:
                  type: string
              description: Pattern represents a pattern in the match transformation.
          fallback_to:
            type: string
          fallback_value:
            type: string
        description: MatchTransform defines a match transformation.
      convert:
        type: object
        properties:
          to_type:
            type: string
            description: bool, float64, int, etc.
          format:
            type: string
            title: '"json" or "quantity"'
        description: ConvertTransform defines a conversion transformation.
    description: Transformer represents a transformation to be applied.
  v1beta1Version:
    type: object
    properties:
      name:
        type: string
      schema:
        type: string
    description: Defines each version of the CRD.
  v1beta1XrdSpec:
    type: object
    properties:
      group:
        type: string
      names:
        type: object
        properties:
          kind:
            type: string
          plural:
            type: string
        description: Names of the resource in singular, plural, and kind forms.
      versions:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
            schema:
              type: string
          description: Defines each version of the CRD.
    description: Main CompositeResourceDefinition spec structure.
  v1beta1QueryListRegistryResponse:
    type: object
    properties:
      registry:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
            provider:
              type: string
            id:
              type: string
              format: uint64
            environment_id:
              type: string
              format: uint64
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  v1beta1QueryShowRegistryResponse:
    type: object
    properties:
      registry:
        type: object
        properties:
          name:
            type: string
          provider:
            type: string
          id:
            type: string
            format: uint64
          environment_id:
            type: string
            format: uint64
  v1beta1Registry:
    type: object
    properties:
      name:
        type: string
      provider:
        type: string
      id:
        type: string
        format: uint64
      environment_id:
        type: string
        format: uint64
